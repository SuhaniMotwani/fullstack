// backend/src/apps/accounts/service.js
const accountsModel = require('./model');

// TODO: Accounts business logic service
const listAccounts = async (organizationId) => {
  // TODO: Fetch accounts via model
};

module.exports = {
  listAccounts,
};
