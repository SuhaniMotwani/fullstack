// backend/src/apps/tasks/service.js
const tasksModel = require('./model');

// TODO: Tasks business logic service
const listTasks = async (organizationId) => {
  // TODO: Fetch tasks via model
};

module.exports = {
  listTasks,
};
