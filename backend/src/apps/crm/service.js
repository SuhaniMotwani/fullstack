// backend/src/apps/crm/service.js
const crmModel = require('./model');

// TODO: CRM business logic service
const listLeads = async (organizationId) => {
  // TODO: Fetch leads via model
};

module.exports = {
  listLeads,
};
