// backend/src/apps/crm/controller.js
const crmService = require('./service');

// TODO: CRM route handler controllers
const getLeads = async (req, res, next) => {
  // TODO: Call crmService and return response
};

module.exports = {
  getLeads,
};
