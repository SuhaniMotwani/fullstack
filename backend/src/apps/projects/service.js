// backend/src/apps/projects/service.js
const projectsModel = require('./model');

// TODO: Projects business logic service
const listProjects = async (organizationId) => {
  // TODO: Fetch projects via model
};

module.exports = {
  listProjects,
};
