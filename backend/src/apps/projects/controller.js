// backend/src/apps/projects/controller.js
const projectsService = require('./service');

// TODO: Projects route handler controllers
const getProjects = async (req, res, next) => {
  // TODO: Call projectsService and return response
};

module.exports = {
  getProjects,
};
