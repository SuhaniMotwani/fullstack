---
description: Never run git commit, git push, or any command that changes version control state
---
Do not run `git commit`, `git push`, `git add`, or any command that stages or
publishes changes to version control. Editing files in the working directory
is fine. Committing and pushing are done manually by the user only.